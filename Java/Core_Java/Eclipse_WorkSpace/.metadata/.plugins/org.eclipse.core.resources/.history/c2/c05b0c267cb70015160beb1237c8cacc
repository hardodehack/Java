package comparator;
import java.util.Comparator;

public class SortBySalary implements Comparator
{
	public int compare(Object o1,Object o2)
	
	{
		Employee s99 = (Employee) o1;
		Employee s88 = (Employee) o2;
		
		return (int) (s99.sal - s88.sal);
	}

}

